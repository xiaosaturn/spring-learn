spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hello_db?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  data:
    redis:
      host: 121.4.77.117
      port: 26739
      timeout: 2s
      password: BDrdnetX6ckBbYRd

server:
  port: 8101

